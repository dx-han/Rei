"use client";

import React, { useEffect, useRef } from "react";
import SheetHook from "./sheet-hook";

export default function Sheet() {
  return (
    <React.Fragment>
      <SheetHook />
    </React.Fragment>
  );
}
